# クライアント／ポジションプロファイルエージェント

クライアント企業ごとに **Craft** にハイブリッド構造のドキュメントを置き、Notion + Salesforce + 打ち合わせメモ + 過去推薦履歴を集約する。マッチング評価の基礎データとなる。

`/client {社名}`、`/position {社名} {ポジション}`、`/match {候補者}` などのスキルから利用される。

---

## 0. 重要：プライバシー

- クライアント／ポジションプロファイルは **Craft 上にのみ保存**する。git・GitHub・他クラウドへの転載は禁止。
- クライアント側担当者の氏名・連絡先は Craft 内で扱うが、対外共有時は本人同意確認。
- 候補者プロファイル（`agents/candidate-profile.md`）とは別ファイルで管理するが、マッチング時には両者を参照する。

---

## 1. 保存先（Craft）

- **トップフォルダ:** `work > 10_Recruitment > 12_Client｜企業`
  - Craft folder ID: `F41A3C28-9B9B-47C7-BAC1-0D8431AF73A9`
- **各社のサブフォルダ:** `{社名}/`（例: `ギフティ/`）
- **その配下：**
  - `{社名}.md` — クライアント md（会社プロファイル＋関係性＋全体パターン）
  - `positions/` — サブフォルダ
    - `{ポジション名}.md` — ポジション md（要件・選考履歴・現パイプライン）

例：
```
12_Client｜企業/
├─ ギフティ/
│  ├─ ギフティ.md
│  └─ positions/
│     ├─ CISO候補.md
│     └─ 企業法務.md
├─ NodeX/
│  ├─ ...
```

既存7件の汎用ドキュメント（`Client List` `JD` `クライアント` 等）はそのまま共存。新規にクライアント md を作る際はサブフォルダで分ける。

---

## 2. 起動モード

| モード | 判定条件 | 振る舞い |
|---|---|---|
| **読み込み** | 会話中でクライアント名／ポジション名が言及された | Craft フォルダを search → 該当ドキュメントを `blocks get --format markdown` で全文取得 |
| **初回生成** | search で該当ドキュメントが見つからない | Notion + SF + 打ち合わせメモ + 過去推薦履歴から横断収集して Craft に新規作成 |
| **対話追記** | ユーザーが素材（打ち合わせメモ・採用要件メモ等）を貼り付け | 該当セクションに `blocks add` で要約追記 |
| **同期更新** | 「最新に同期」「ステータス反映」等の指示 | Notion・SF を再取得して該当セクションを更新 |
| **マッチング評価** | `/match` 系コマンド or「合いそうな案件は？」 | 候補者 md ＋ 全注力ポジション md を読み比較・スコア付け |

---

## 3. データソース

| ソース | 取得方法 | 主な内容 |
|---|---|---|
| Notion 企業DB `collection://1fb7d017-b6a0-80b4-a83a-000b901f891a` | search / fetch | 会社プロフィール、ピッチ資料、関わり度合い、ATS URL |
| Notion ポジションDB `collection://1fb7d017-b6a0-8052-a7c9-000b1aa76cda` | search / fetch | JD・要件・報酬レンジ・注力フラグ・契約形態・料率 |
| Notion パイプラインDB `collection://20f7d017-b6a0-807c-a60f-000b827c6841` | search / fetch（ポジション経由） | 進行中候補者・選考ステータス |
| Notion 面談メモDB `collection://20c7d017-b6a0-8014-82f5-000b750ec0a8` | search / fetch（企業／ポジションリレーション経由） | 採用ヒアリング・面接同席メモ |
| SF Account | salesforce_search_all + ATS_URL__c フィールド | 会社レコード、**ATS URL**（ドメイン抽出元）、契約状況、Notion Page ID |
| SF matching__c | salesforce_query_records WHERE ApplyCompany__c = '{社AccountId}' | **過去全推薦履歴**（脱落理由・パターン分析の宝庫） |
| Slack | slack_search_public_and_private | 社内での議論・推薦相談 |
| **Gmail 個人** (sato-y@pnl.co.jp) | search_threads | 候補者・クライアント担当者との個人窓口やり取り |
| **Gmail 共有由来**（個人 Gmail 内 `SY/` ラベル下に転送・自動振り分け済） | search_threads with `label:SY/{コード}` | **クライアントとの公式やり取り・ATS 通知（HERP・HRMOS・Talentio 等）** |

---

## 4. ドキュメント構造

### 4.1 クライアント md（`{社名}.md`）

```
（タイトル＝ドキュメント名そのもの。例: ギフティ）

> Last synced: YYYY-MM-DD HH:MM (JST)
> Sources: Notion ✓ / Salesforce ✓ / 面談メモ ✓ / 過去推薦履歴 ✓
> Notion: [企業ページ](https://...)
> Salesforce: [Account](https://...)
> 関連リンク: [会社紹介資料](...) / [ATS](...) / [テックブログ](...)

---

## 留意事項・申し送り
（個別配慮：「直接連絡はXさん経由」「Y担当者は慎重派」「コンサル系候補者NG」など）

## 1. 会社概要
- 上場区分 / 事業ドメイン / 事業モデル / 従業員規模 / 創立 / 本社
- 直近売上 / 成長率
- 事業内容（ピッチ資料サマリ）
- 競合 / 競合優位性
- 成長戦略（直近）

## 2. 経営陣・キーパーソン
- 代表 / CFO / CTO 等
- P&L 接点ある担当者：氏名 / 役割 / 性格・スタンスメモ
- 決裁構造（誰が最終承認するか）

## 3. P&L との関係性
- 関わり度合い（高/中/低）
- 契約形態（通常Fee/UpFee/リテイナー）・料率
- **使用 ATS：** {ATS 名（HERP / HRMOS / Talentio / Greenhouse / Lever / persona-ats / jposting / 直メール 等）}
- **ATS URL：** SF Account.ATS_URL__c の値
- **ATS メールドメイン：** {ATS URL から抽出、例：herp.cloud}（Gmail 共有由来フィルタの対象）
- **Gmail 共有由来ラベル：** `SY/{社コード}` および `SY/ATS-{ATS名}`
- 過去の決定（オファー承諾された候補者）
- 直近の打ち合わせ履歴（リンク + 1行要約）

## 4. P&L 経由の推薦傾向（マッチング学習資産）
**通った候補者の特徴：** ...
**見送られた候補者の特徴：** ...
**カルチャー上の地雷：** ...
**報酬ゾーン上の地雷：** ...
**選考プロセス上のクセ：** ...
（具体的な過去事例を引用付きで列挙）

## 5. アクティブポジション
（注力 YES のポジション一覧、md リンク付き）
- [CISO候補](./positions/CISO候補.md) — 注力 / オープン / カジュアル面談3件中
- [企業法務](./positions/企業法務.md) — 注力 / オープン / スカウト開始前

（注力 NO のポジションも軽く列挙、md化はしないが Notion リンクは載せる）

## 6. 打ち合わせメモ要約（新しい順）
- YYYY-MM-DD タイトル — [MEETING-NNN](Notion URL) — 主要トピック

## 7. 過去推薦履歴サマリ
（直近半年〜1年の推薦履歴を、ポジション別にカウント＋成否概要）

## 8. ToDo・次のアクション
- [ ] ...

## 9. 過去のやり取りログ（生データ）
（打ち合わせメモ原文・採用ヒアリングメモなど、参照価値の高い原文）
```

### 4.2 ポジション md（`positions/{ポジション名}.md`）

```
（タイトル＝ポジション名）

> Last synced: YYYY-MM-DD HH:MM (JST)
> Sources: Notion ✓ / SF ✓ / 過去推薦履歴 ✓
> Notion: [ポジションページ](https://...)
> JD: [JDページ](https://...) / [ATS](...)
> クライアント: [{社名}](../{社名}.md)

---

## 留意事項
（このポジション固有の配慮）

## 1. ポジション要件
- 報酬レンジ：下限 X 万円 〜 上限 Y 万円
- 契約形態 / 料率
- ATS URL
- 採用背景 / 求める役割
- Must-have スキル：
- Nice-to-have スキル：
- カルチャーフィット要件：
- 注力フラグ・ステータス

## 2. JD 抜粋
（重要部分の要約 or 全文）

## 3. P&L 側の解釈・差別化ポイント
- 候補者に響くピッチ要素
- 競合ポジションとの違い
- 訴求すべき魅力

## 4. 現パイプライン
（進行中の候補者一覧、ステータス、直近アクション）

## 5. 過去推薦履歴と結果
| 日付 | 候補者 | ステータス | 結果 / 理由 |
|---|---|---|---|
| YYYY-MM-DD | X様 | 脱落 | 応募意思フェーズで連絡途絶 |
| ... | ... | ... | ... |

## 6. このポジション固有の脱落パターン
- 「XX出身は技術面で見送り傾向」
- 「YY歳以上はカルチャーフィット難」
- など

## 7. スカウト文・推薦状の蓄積
- [v1 スカウト文](Craft/Notion URL)
- 効果のあった訴求軸：...

## 8. ToDo
- [ ] ...
```

---

## 4.3 Gmail 検索戦略（個人 + 共有由来の2系統）

会社の共有 Gmail は別アカウントで Claude から直接見れないため、**個人 Gmail（sato-y@pnl.co.jp）への自動転送＋ラベル振り分け**で同期している。

### ラベル設計

- **個人 Gmail 由来：** プレフィックスなし。例：`GFT`（ギフティ）、`KHS`（カケハシ）
- **共有 Gmail 由来：** `SY/` 配下にネスト。例：
  - `SY/GFT` — ギフティ関連の共有メール（クライアントからの公式やり取り、ポジション情報など）
  - `SY/ATS-HERP` — HERP 系 ATS 通知（複数クライアント横断）
  - `SY/ATS-HRMOS` — HRMOS 系 ATS 通知
  - `SY/ATS-Talentio` 等

### 共有 Gmail 側のフィルタ Recipe

クライアントごと（注力クライアントのみ）：
```
Has the words: "{社名}" OR "{英語社名}"
→ 転送: sato-y@pnl.co.jp
→ ラベル: SY/{社コード}
```
※ Gmail の「Has the words」は subject + body + snippet を検索

ATS 共通フォールバック（全クライアント横断、7 ATS 系統を一括）：
```
from:(*@herp.cloud) OR from:(*@hrmos.co) OR from:(*@talentio.com)
  OR from:(*@greenhouse.io) OR from:(*@lever.co)
  OR from:(*@persona-ats.com) OR from:(*@jposting.net)
→ 転送: sato-y@pnl.co.jp
→ ラベル: SY/ATS（または ATS 種別ごとに分割可）
```

### 検索順序（マッチング評価・候補者プロファイル生成時）

1. **個人 Gmail（直接やり取り）：** `search_threads` with `{候補者メアド}` or `{候補者氏名}`
2. **共有 Gmail 由来（クライアント文脈）：** `search_threads` with `label:SY/{社コード} "{候補者姓}"`
3. **共有 Gmail 由来（ATS 通知）：** `search_threads` with `label:SY/ATS "{候補者姓}" "{社名}"` （ATS 通知は subject/body に **{ATS_ID}@{ATSドメイン}** 形式の sender で来る、候補者氏名は subject に含まれる前提）

### 重要：sender アドレスベースでの候補者識別はしない

ATS 通知の sender は `{ランダム候補者ID}@{ATSドメイン}` 形式で、毎回違うため。**識別は subject（企業名 + 候補者名）または body 内容**で行う。

### クライアント名・候補者名の判別パターン（共有 Gmail）

| 種別 | 判別 | 検索クエリ例 |
|---|---|---|
| 候補者メール | subject に企業名 + 候補者名 | `label:SY/GFT "八木 俊輔"` |
| ポジション開閉（subject 型） | subject に企業名 | `label:SY/GFT (closed OR open OR クローズ OR オープン)` |
| ポジション開閉（body 型） | 本文に企業名 | `label:SY/ATS "ギフティ"` |

---

## 5. 同期戦略（重要）

候補者数・ポジション数の規模を考慮し、**コスト×スピードのバランス**で2層運用：

| 区分 | 保管方法 | 同期方法 | マッチング時 |
|---|---|---|---|
| **注力 = YES** ポジション | Craft に md として常設 | `/position-sync` で手動同期 or 重要更新時に Claude が自動 | md を直接読む |
| **注力 = NO** ポジション | Craft 化しない | 都度 Notion / SF から fetch | マッチング時に Notion ポジション DB を query |

**根拠：**
- 注力ポジションは P&L 独自の知見が厚く、永続化のリターン大（20件程度）
- 非注力は数が多く（100件）、SF が真の最新ソース。md 化はコスパ悪い
- マッチング時は「注力 md 全件 + 必要なら非注力スナップショット」の二段取り

「注力 → 非注力」または「注力 → クローズ」フラグ変化時：md を Archive サブフォルダに移動。

---

## 6. マッチング履歴の蓄積場所（学習資産）

過去推薦の成否・理由を以下2レベルに分けて蓄積：

| 配置先 | 内容 | 例 |
|---|---|---|
| **ポジション md「過去推薦履歴と結果」** | この具体ポジションへの推薦実績・結果・理由 | 「2026-04 X様推薦→技術面で見送り（クラウドネイティブ経験不足）」 |
| **クライアント md「P&L 経由の推薦傾向」** | このクライアント全体への通底パターン | 「コンサル出身者はカルチャーフィット難」「会社名だけ聞いてくる候補者はNo Show率高い」 |

**昇格ルール：**
- ポジション md の履歴が複数件たまり、パターンとして抽象化できる段階でクライアント md に昇格
- 同社の別ポジションで再び現れたパターンは即昇格

---

## 7. マッチング評価（`/match` フロー）

### 候補者起点（`/match {候補者姓}` または自然言語）

1. Craft から候補者 md を fetch
2. Craft から全注力ポジション md を一覧 → 各 md を fetch
3. （必要なら）Notion ポジション DB から注力 NO の active ポジションも取得
4. 各ポジションについて、以下5軸で構造化評価：
   - **要件マッチ度**（Must-have の充足度）
   - **カルチャーマッチ度**（本人志向 × クライアント文化、過去通底パターン参照）
   - **年収マッチ度**（本人希望 × ポジション提示レンジ）
   - **フェーズマッチ度**（本人のフェーズ適性 × 企業ステージ）
   - **過去パターン**（このクライアント／類似ポジションでの脱落パターンに照らした懸念点）
5. ランキングして上位3〜5件を提案。各候補について：
   - 推薦時に強調すべき点
   - 懸念点／事前に詰めるべきこと
   - 推薦の進め方（具体的なアクションリスト）

### ポジション起点（`/match-position {社名} {ポジション}`）

1. 該当ポジション md を fetch
2. Craft の全候補者 md を一覧 → 各 md を fetch（規模大きければ active flag で絞る）
3. 各候補者について同5軸で評価、ランキング

---

## 8. 出力時のチェックリスト

- [ ] Craft フォルダ `12_Client｜企業/{社名}/` 配下に作成されている
- [ ] クライアント md とポジション md（注力 YES のみ）が揃っている
- [ ] 最上部の Last synced / Sources / Notion・SF URL が埋まっている
- [ ] 「P&L 経由の推薦傾向」セクションに過去履歴の抽象化された学びが入っている
- [ ] 個人情報を `clients/` ローカルや git に書き出していない

---

## 9. NG

- クライアント情報を git にコミット・push しない
- クライアントの内部議論（特に経営陣のキャラ評価・選考プロセスの不備指摘）を外部共有しない
- 注力 NO のポジションを md 化しない（コスト過剰）
- マッチング評価で根拠なくスコアを付けない（必ず引用と論理を併記）
